spring:
  datasource:
    url: jdbc:mysql://localhost:3306/todolist
    username: root
    password: samuraisam7

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

